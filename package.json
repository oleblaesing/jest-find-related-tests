{
  "private": true,
  "name": "jest-find-related-tests",
  "version": "1.0.0",
  "dependencies": {
    "jest": "^23.6.0"
  },
  "scripts": {
    "find": "jest --findRelatedTests",
    "watch": "jest --watch"
  }
}
